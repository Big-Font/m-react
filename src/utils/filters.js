/* ========================================================
    jason
    2019/2/23
    过滤器函数
   ====================================================== */

export function date(input) {
  if(!input) {
    return ''
  }
  let arr = input.split(' ')[0].split('-');
  return `${arr[0]}年${arr[1]}月${arr[2]}日`;
}
